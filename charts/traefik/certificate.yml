---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned
spec:
  selfSigned: {}

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik.mrry.io
  namespace: traefik-system
spec:
  dnsNames:
    - traefik.mrry.io
  secretName: traefik.mrry.io
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer

