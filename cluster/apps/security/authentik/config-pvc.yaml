---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik-config
  namespace: security
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 4Gi
  storageClassName: rook-cephfs
