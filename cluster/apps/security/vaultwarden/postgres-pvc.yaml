---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaultwarden-postgres
  namespace: security
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 8Gi
  storageClassName: rook-cephfs
